function sum(numOne, numTwo, numThree){
    const sum = (numOne ,numTwo) => numOne + numTwo;
    const subtract = (sum(numOne, numTwo) - numThree);
    console.log(subtract);
}
sum(42,
    58,
    100    
    )